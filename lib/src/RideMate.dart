import 'package:flutter/material.dart';

class RideMate extends StatelessWidget {
    const RideMate({super.key});

    @override
    Widget build(BuildContext context) {
        return WidgetsApp(title: "Ride Mate", color: const Color(255),);
    }
}
